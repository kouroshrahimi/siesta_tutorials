# Check convergence of total energy vs. LatticeConstant 
This script automates the convergence of total energy versus `LatticeConstant` parameter. Make it executable:
```bash
$ chmod +x lattice_constant_convergence.sh
```
Then you can run it:
```bash
$ ./lattice_constant_convergence.sh
```
