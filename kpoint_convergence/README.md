# Check convergence of total energy vs. kpoint 
This script automates the convergence of total energy versus `kpoint` parameter. Make it executable:
```bash
$ chmod +x kpoint_convergence.sh
```
Then you can run it:
```bash
$ ./kpoint_convergence.sh
```
